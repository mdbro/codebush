# CodeBush — current brief

Build a native Rust explorer for this workstation’s RTX 5090, targeting 7680 × 4320 at 120 rendered frames per second with up to 32 decimal GB of application GPU allocations. Show full source text, syntax coloring, and a tests toggle. Keep the separate directory atlas available through a GUI mode switch and `--tree`. The user now explicitly asks to surpass the supplied video's readability: distinguish file surfaces from adjacent files and the canvas, preserve readable source on every surface, and clearly identify directory hierarchy. Use tall source columns, continuing left to right inside a panel when necessary; never truncate source. Keep source panels inside the viewport throughout rotation without manual refitting.

The current default extends the atlas into a fixed Euclidean 12D reference graph. The user explicitly approved a deterministic partition of directed references across all 66 coordinate planes. Each plane includes only its links and incident files. Plane membership is a graph filter; it is not a claim about an edge’s axis support. Use proper orthogonal rotations, continuous intermediate projections, and readable 2D annotations at fixed projected file anchors. No perspective camera or intermediate 3D scene.

Full source and graph assets for tests on/off stay resident after load. Do not silently omit source to meet a budget or fabricate unresolved dependencies. Report static resolver limitations and exact source/graph counts. Source indexing, layout, and reference preparation can use the full CPU and RAM resources. Navigation uses resident GPU assets.

The supplied WbPqpS1ZwNfFPJj3.mp4 concept was inspected at 0, 1, 6, 13, 20, 30, and 40 seconds. It informed the persistent source-panel and overview-to-reading interaction. The later 12D requirement supersedes the original brief’s restriction on graph rotation.

Test this project and different-sized real open-source codebases, including rust-lang/rust. Record actual adapter, 8K resolution, individual CPU/GPU/completion timings, allocation estimates, screenshots, and rendering errors. Average frame cadence is insufficient to certify a steady 120fps. The attached 4K/60 desktop does not certify physical 8K/120 output.

Independent critic: score each criterion from 0–10; pass requires at least 8.5 and zero rendering errors. The earlier four-round reviews remain historical failures. The latest visual repair completed all five authorized adversarial critic reviews, ending at 8.51/10 with zero observed rendering errors; see VISUAL-REPAIR.md. That five-review cycle is complete. Judge the main visualization against the actual reference, including small, medium and large codebases, useful overview composition, source contrast, hierarchy, reading, and navigation. Decorative controls and engineering evidence cannot compensate for a failed main visualization. Only the critic may be delegated. Keep subsequent evidence separate from that completed cycle; earlier performance and reference-completeness failures remain explicit until requalified.
